import 'package:flutter/material.dart';

class MapsPageDesktop extends StatelessWidget {
  const MapsPageDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
