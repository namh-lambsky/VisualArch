import 'package:flutter/material.dart';

class ProfileMenuDesktop extends StatelessWidget {
  const ProfileMenuDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
