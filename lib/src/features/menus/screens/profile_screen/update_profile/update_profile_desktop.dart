import 'package:flutter/material.dart';

class UpdateProfileDesktop extends StatelessWidget {
  const UpdateProfileDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
