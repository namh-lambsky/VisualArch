import 'package:flutter/material.dart';

class MainMenuDesktop extends StatelessWidget {
  const MainMenuDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
