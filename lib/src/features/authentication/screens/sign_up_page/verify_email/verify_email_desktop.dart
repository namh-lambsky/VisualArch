import 'package:flutter/material.dart';

class VerifyEmailDesktop extends StatelessWidget {
  const VerifyEmailDesktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
